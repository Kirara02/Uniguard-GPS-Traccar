enum DrawerPage {
  devices('Devices'),
  settings('Settings'),
  logout('Logout');

  // Properti untuk menyimpan title
  final String title;

  // Constructor untuk enum
  const DrawerPage(this.title);
}
